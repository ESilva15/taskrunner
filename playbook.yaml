playbook_name: "test_playbook"
registries:
  - "./registry/directory_actions.py"
  - "./registry/borg_actions.py"

steps:
  - name: "backup dir1"
    actions:
      pre: "directory_exists"
      play: "copy_directory"
      post: "verify_copy"
    context:
      source_dir: "./dir1"
      destiny_dir: "./dir2"
  - name: "backup dir2"
    actions:
      pre: "directory_exists"
      play: "copy_directory"
      post: "verify_copy"
    context:
      source_dir: "./dir1"
      destiny_dir: "./dir2"
