[project]
name = "taskrunner"
version = "0.1.0"
description = "Utility to run tasks such as backups"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "docker>=7.2.0",
    "pyaml>=26.7.0",
    "pytest>=9.1.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
