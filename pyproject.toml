[project]
name = "taskrunner"
version = "0.1.0"
description = "Utility to run tasks such as backups"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.pyright]
venvPath = "."
venv = ".venv"
